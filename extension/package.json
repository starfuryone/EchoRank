{
  "name": "echorank-extension",
  "version": "0.1.0",
  "private": true,
  "description": "EchoRank browser extension — import reviews from Google, Facebook, and Trustpilot.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "zip": "node build.mjs && cd dist && zip -r ../echorank-extension.zip . && cd .."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
